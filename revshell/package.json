{
  "name": "revshell-for-ctfbot",
  "version": "1.0.0",
  "description": "Reverse shell for CTF bot",
  "license": "BSD-2-Clause",
  "author": "Tafka4",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "start": "tsc && node dist/index.js",
    "build": "rm -rf dist && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^17.2.2",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.6.1"
  }
}
